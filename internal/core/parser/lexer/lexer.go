package lexer

import "github.com/twodigitss/apio/internal/core/parser/models"

// block (string) is the specific request block the splitter returns and HAS new lines
func Lexer(block string) (models.Tokens, error) {
	return models.Tokens{}, nil
}
